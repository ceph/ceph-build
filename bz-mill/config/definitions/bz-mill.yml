- job:
    name: bz-mill
    defaults: global
    display-name: 'bz-mill'

    triggers:
      - timed: '@hourly'

    builders:
      - shell:
          !include-raw:
            - ../../build/generate_map_namespace_to_query.sh
            - ../../build/build
